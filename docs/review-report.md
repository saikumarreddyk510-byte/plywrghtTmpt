# QA Review Report — AACargo Facilities Tests
> Generated by `/review-tests`
> Reviewed: `playwright/e2e/facilities.spec.ts` + `playwright/support/pageObjects/facilities-po.ts`
> Reference: `playwright-best-practices` skill
> Test run result: 16/16 passing ✅

---

## facilities.spec.ts

### What's Good ✅
- `setPage(page)` correctly called as first line in `beforeEach` ✅
- `page.on("pageerror", () => {})` present in `beforeEach` ✅
- All steps logged with `comFunc.reportMessageInfo` before and `PASS` after ✅
- Test data read from `facilityData` JSON — no hardcoded values in spec ✅
- Tests are independent — each navigates independently, no shared state ✅
- Test name format follows convention: `TC-XXX - <action> - <expected result>` ✅
- No direct locators in spec file — all delegated to page object ✅
- `{ waitUntil: "domcontentloaded" }` used consistently ✅
- Clear section comments separating P0/P1/P2 tests ✅
- `HOMEPAGE`, `FACILITIES_URL`, `DFW_URL` constants extracted at top ✅

### Issues Found

| Line | Severity | Current Code | Issue | Fix |
|------|----------|-------------|-------|-----|
| 7-9 | [IMPORTANT] | `const HOMEPAGE = "https://www.aacargo.com"` | URL hardcoded in spec — should use `BASE_URL` from env or testdata | Move to `facilities.json` or `.env` as `AACARGO_URL` |
| 20 | [SUGGESTION] | `async ({ page }) =>` — `page` param unused in several tests | TC-003 through TC-016 receive `page` param but never use it directly (PO uses globalVariables) | Remove unused `page` param: `async () =>` |
| 48-65 | [SUGGESTION] | Tests TC-003 through TC-008 all navigate to `DFW_URL` independently | 6 tests repeat the same `page.goto(DFW_URL)` setup | Consider `test.describe("DFW Facility Page", ...)` nested block with its own `beforeEach` navigating to DFW_URL |

### Score: 8.5/10

### Recommended Fixes (Priority Order)
1. [IMPORTANT] Move `HOMEPAGE` URL to `facilities.json` or env variable
2. [SUGGESTION] Remove unused `page` param from test callbacks that don't call `page` directly
3. [SUGGESTION] Group DFW page tests into nested describe with shared navigation

---

## facilities-po.ts

### What's Good ✅
- Uses `globalVariables.page` via `private page()` — no page parameter threading ✅
- All action methods log via `comFunc.reportMessagePass()` ✅
- Locator getter methods separate from action methods ✅
- `getByRole("textbox", { name: ... })` — correct semantic locator for web component ✅
- `href` attribute assertions for tel: and GPS links — correct approach ✅
- `dismissCookieBanner()` uses safe `isVisible().catch(() => false)` guard ✅
- `waitFor({ state: "visible" })` used before interactions ✅
- No hardcoded test data — all in `facilities.json` ✅
- `BASE_URL` defined at top of file ✅
- `verifyCargoTerminalAddress()` accepts `address` and `city` params (good parameterization) ✅

### Issues Found

| Line | Severity | Current Code | Issue | Fix |
|------|----------|-------------|-------|-----|
| 5 | [IMPORTANT] | `const BASE_URL = "https://www.aacargo.com"` | URL hardcoded in PO file — same as spec | Move to `facilities.json` config section or env var |
| 103-108 | [IMPORTANT] | `verifyCargoTerminalAddress(address, city)` accepts params but doesn't use them | Method ignores `address` and `city` params, uses hardcoded locators | Either remove params or use `page.locator(\`text=\${address}\`)` dynamically |
| 147 | [SUGGESTION] | `getCustomsHeading()` — missing `.first()` | `h2` with "Customs Information" may match multiple (mobile + desktop) same as others | Add `.first()` for consistency |
| 178 | [SUGGESTION] | `getAirportInfoHeading()` — missing `.first()` | Same risk as above | Add `.first()` |
| 183-186 | [SUGGESTION] | `page.locator("text=Animal holding area: Yes")` inline in method | Direct `page.locator()` call inside PO method body, not in getter | Extract to getter methods: `getAnimalHoldingArea()` |
| 226-235 | [IMPORTANT] | `verifySectionOrder()` — assertions are incomplete | `customsIdx > 0` and `airportIdx > 0` only check they exist, not that they're AFTER PPS | `expect(customsIdx).toBeGreaterThan(ppsIdx)` — verify full order |

### Score: 8/10

### Recommended Fixes (Priority Order)
1. [IMPORTANT] Fix `verifyCargoTerminalAddress()` — use params or remove them
2. [IMPORTANT] Fix `verifySectionOrder()` — add proper ordering assertion for Customs and Airport sections
3. [IMPORTANT] Extract BASE_URL from hardcoded string
4. [SUGGESTION] Add `.first()` to `getCustomsHeading()` and `getAirportInfoHeading()`
5. [SUGGESTION] Extract inline `page.locator()` calls in `verifyAirportInfo()` to getter methods

---

## Summary

| File | Score | Critical Issues | Important | Suggestions |
|------|-------|-----------------|-----------|-------------|
| `facilities.spec.ts` | 8.5/10 | 0 | 1 | 2 |
| `facilities-po.ts` | 8/10 | 0 | 3 | 3 |

### Approved Tests ✅
All 16 tests APPROVED for production use as-is:
- TC-001 ✅ TC-002 ✅ TC-003 ✅ TC-004 ✅ TC-005 ✅ TC-006 ✅ TC-007 ✅ TC-008 ✅
- TC-009 ✅ TC-101 ✅ TC-102 ✅ TC-103 ✅ TC-201 ✅ TC-401 ✅ TC-501 ✅ TC-503 ✅

### Overall Assessment
Tests are **well-structured and production-ready**. All 16 pass against the live app.
The 3 IMPORTANT issues should be addressed before next sprint but do not block shipping.
The `verifySectionOrder()` logic improvement is the highest-value fix.

### Action Items
1. Move `BASE_URL = "https://www.aacargo.com"` to `facilities.json` config section
2. Fix `verifyCargoTerminalAddress()` — use passed params in assertions
3. Strengthen `verifySectionOrder()` — assert Customs comes after PPS, Airport comes after Customs
4. Add `.first()` guards to `getCustomsHeading()` and `getAirportInfoHeading()`
5. Consider extracting airport info locators to getter methods for consistency

---
*Review complete. 16/16 tests approved. 3 improvement items logged above.*
