# Review Report — `clientLogin.spec.ts`

> Generated by `/review-tests playwright/e2e/clientLogin.spec.ts
> playwright/support/pageObjects/clientLogin-po.ts`

## What's Good ✅
- `setPage(page)` and `page.on("pageerror", () => {})` present and first, as required
- Every action logged via `comFunc.reportMessage*` — clean before/after trail
- All locators live in the Page Object; the spec never calls `page.locator()` directly
- Locator priority followed: IDs (`#userEmail`, `#userPassword`, `#login`) used
  wherever one exists; `.toast-message` (class selector, lowest priority) is
  used only for the toast, which has no ID/role/label alternative — a
  legitimate exception, not a violation
- No `page.waitForTimeout()` anywhere
- Both TCs from `docs/test-strategy.md` implemented, no duplicates

## Issues Found (and fixed before this report)
| Severity | Issue | Fix applied |
|----------|-------|-------------|
| [IMPORTANT] | TC301 hardcoded `"nonexistent-user-xyz@test.com"` / `"WrongPass@123"` directly in the spec, violating "all data from `testdata/*.json`" | Added an `invalidUser` entry to `playwright/testdata/users.json`, spec now imports it |
| [IMPORTANT] | TC001 verified success only via `waitForURL`, with no explicit `expect()` — the checklist requires at least one meaningful assertion per test | `verifyOnDashboard()` now also asserts `expect(page).toHaveURL(...)` after the wait |

## Cross-tool finding (not specific to this file)
`create-scenarios`' own numbering convention (`TC-001`, `TC-301` — dashed,
3-digit, per its lens ranges) doesn't match `playwright-best-practices`' test
title template (`TC01 - ...` — no dash, 2-digit). This spec follows the
best-practices template for the actual test titles (`TC001`, `TC301`) while
`docs/test-scenarios.md` uses the create-scenarios format (`TC-001`,
`TC-301`) — harmless here since both map 1:1, but worth reconciling in the
skill files themselves so a future contributor doesn't have to notice the gap
by hand, the way this run just did.

## Score: 9/10
Both issues found were real and are fixed in the files being reviewed, not
just noted. The one point held back is the cross-tool numbering
inconsistency above — a skill-authoring gap, not a defect in this spec.

## Recommended Fixes (Priority Order)
1. [DONE] Move TC301's test data into `testdata/users.json`
2. [DONE] Add an explicit `expect()` to TC001's success path
3. [SUGGESTION] Reconcile `TC-NNN` vs `TCNN` numbering between
   `create-scenarios` and `playwright-best-practices`
